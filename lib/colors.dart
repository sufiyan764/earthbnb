import 'package:flutter/material.dart';

// Airbnb-Inspired Theme Colors
class AppColors {
  // Primary Color
  static const Color primary = Color(0xFFFF5A5F);

  // Accent Colors
  static const Color accentTeal = Color(0xFF00A699);
  static const Color lighterAccentTeal = Color(0x80A6E4E0); // 50% opacity




  static const Color accentOrange = Color(0xFFFC642D);

  // Background Colors
  static const Color backgroundLightGray = Color(0xFFF7F7F7);
  static const Color backgroundWhite = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textDarkGray = Color(0xFF484848);
  static const Color textLightGray = Color(0xFFB0B0B0);

  // Unselected Navigation Icon Color
  // static const Color unselectedNavIcon = Color(0xFFB0B0B0);
  static const Color unselectedNavIcon = Color(0xFFFFFFFF);

  // Card Backgrounds
  static const Color cardBackground = Color(0xFFFFFFFF); // White for individual property cards
  static const Color cardShadow = Color(0x22000000); // Subtle shadow effect

  // Button Colors
  static const Color buttonTeal = Color(0xFF00A699);
  static const Color buttonOrange = Color(0xFFFC642D);

  // Divider Color
  static const Color divider = Color(0xFFE0E0E0);

  // Placeholder colors
  static const Color placeholderIcon = Color(0xFFB0B0B0);
}
